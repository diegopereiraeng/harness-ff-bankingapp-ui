{
  "name": "harness-ff-bankingapp-ui",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "lint": "eslint --ext .js ./ --format junit --output-file ./reports/eslint/eslint.xml",
    "lint:fix": "npm run lint -- --fix",
    "coverage": "echo \"running coverage test!!\"",
    "coverage:drone": "npm run coverage"
  },
  "devDependencies": {
    "eslint": "^7.31.0",
    "lint": "^0.7.0"
  }
}
